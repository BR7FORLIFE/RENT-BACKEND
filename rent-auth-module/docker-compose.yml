services: 
  auth-database:
    image: mongo:noble
    container_name: auth-mongo
    ports:
      - 27017:27017
    volumes: 
      - mongo_data:/data/db
    env_file:
      - rent-auth-module/.env

volumes: 
  mongo_data: